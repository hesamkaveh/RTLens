{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the HUD and settings windows. The plugins RTLens uses (global shortcut, store, autostart, opener) are driven entirely from Rust, so the frontend needs no capability for them.",
  "windows": ["hud", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
